import { SearchVehiclePipe } from './search-vehicle.pipe';

describe('SearchVehiclePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchVehiclePipe();
    expect(pipe).toBeTruthy();
  });
});
